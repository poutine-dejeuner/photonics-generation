# @package _global_

defaults:
  - override /model: normflow
  - override /inference: normflow

experiment:
  name: "normflow_ema_comparison"
  description: "Normalizing Flow with EMA model comparison with diffusion models"
  tags: ["normflow", "ema", "comparison"]

model:
  ema_decay: 0.9999
