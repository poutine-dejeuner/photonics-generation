# @package _global_

defaults:
  - override /model: vae
  - override /train: vae
  - override /inference: vae

experiment:
  name: "vae_comparison"
  description: "VAE model comparison with diffusion models"
  tags: ["vae", "comparison", "beta-vae"]

model:
  beta: 1.0  # Standard VAE, can be changed to β-VAE
