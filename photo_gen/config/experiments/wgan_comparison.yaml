# @package _global_

defaults:
  - override /model: wgan
  - override /train: wgan
  - override /inference: wgan

experiment:
  name: "wgan_comparison"
  description: "WGAN-GP model comparison with diffusion models"
  tags: ["wgan", "comparison", "gradient-penalty"]

model:
  lambda_gp: 10  # Gradient penalty coefficient
