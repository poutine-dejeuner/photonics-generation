# @package _global_

defaults:
  - override /model: standard_gan
  - override /train: standard_gan
  - override /inference: standard_gan

experiment:
  name: "standard_gan_ema_comparison"
  description: "Standard GAN with EMA model comparison with diffusion models"
  tags: ["standard_gan", "ema", "comparison"]

model:
  ema_decay: 0.9999
